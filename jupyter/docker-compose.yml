version: '3.5'
services:
  jupyter:
    image: lobnek/jupyter
    build:
       context: .
       dockerfile: jupyter/Dockerfile
    volumes:
       - /media/j/notebooks:${WORK}
    ports:
       - ${PORT}:8888
